# Neon Postgres (from console or `neon connection-string`)
# Paste your pooled connection string from the Neon Console
DATABASE_URL=

# Neon AI Gateway — project must be in aws-us-east-2 (Ohio)
# Get from: Console → Project → AI Gateway → Credentials
NEON_AI_GATEWAY_TOKEN=
NEON_AI_GATEWAY_BASE_URL=

# Neon project metadata (for branch setup script)
NEON_PROJECT_ID=
NEON_API_KEY=
